
export interface Segment {
  id: string;
  label: string;
  color: string;
  points: number;
}

export interface HistoryItem {
  id: string;
  label: string;
  points: number;
  timestamp: number;
}
